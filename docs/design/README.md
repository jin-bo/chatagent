# Design docs index · 设计文档索引

Grouped map of `docs/design/`. Docs are **not** moved into subdirectories — they
cross-link each other heavily (`Companion:` / `Related:` frontmatter) and several
are cited as anchors by other docs and by agent memory, so flattening keeps those
links stable. Use this index to find the **review records / backlog** vs the
**active designs** vs the **landed contracts**.

本目录不做物理分目录：这些文档彼此交叉链接密集，且部分被其它文档与记忆当锚点引用，扁平结构能让链接
稳定。用本索引区分「评审记录 / backlog」「活跃设计」「已落地契约」。

> Each doc has a `.zh.md` companion **except** those marked *(en only)*.
> 除标注 *(en only)* 外，每篇都有 `.zh.md` 对照件。

---

## Foundational / orientation · 基础与定位

Start here — strategy and the host/ACP surface boundary.

- **path-a-roadmap** — embed-first strategy (locked 2026-04-30); the anchor most other records test against.
- **embedding-vs-acp** — decision tree: which integration surface (embed / `agentao run` / ACP) do I use?
- **embedded-host-contract** *(en only)* — the public host-contract design (events, ActivePermissions, ACP schema).

## Active & proposed designs · 活跃与提案中的设计

The live backlog of work — proposed, in progress, or impl-deferred. New build work usually starts from one of these.

- **hooks-claude-contract-conformance-plan** — ***implemented and merged**, rev 24 — 2026-08-30, PR #199 / `18fb628`, **unreleased** (0.4.21 dev cycle); all seven steps and all ten gates, §0 is the closure index. 7 new modules under `agentao/plugins/hooks/`, 11 new test files, +6557/-429 over 44 files, full suite 4590 green. rev 22 passed the twenty-first review with no findings; rev 23 closed the gates* — a **versioned compatibility layer** closing the deviations catalogued in **hooks-three-way-claude-codex-agentao** (which stays analysis-only). Promise: agentao implements a *declared profile* of the Claude hook contract — the event list is shorter, the field list is **enumerated**, and within the profile every event obeys the documented contract **in configuration and on the wire**. Two contracts side by side (`agentao-v1` frozen, `claude-code@profile-1`), resolved **per file by shape detection** because a copied Claude `hooks.json` carries no `contract` key — and parsed to **zero rules** under agentao before this landed — the deviation upstream of the comparison's nine. Mechanism: a `ParsedHookOutput` / `ResolvedHookOutput` split, a `resolve(event, returncode, stdout, stderr)` branching over `{0, 2, other}` × **five** stdout states, four field matrices (output profile, event × universal-field delivery, input per event, mixed-contract control lattice), **ten design gates**, and seven steps each carrying its own tests. The doc's own **revision history table** records what each of the twenty-one rounds found — **five reversals** (rev 3, 6, 10, 11, 14) and **nine rounds where the previous revision's own new material broke a rule the document already carried** (rev 9, then 13 through 20 unbroken). Two contested rows (`SessionStart`'s `continue:false`, `PostToolUseFailure`'s `decision`) are resolved *against* the reference's wider statement and each carries a **flip list** — every section that changes if G7's probe reverses it, because "invert the test" is not a plan. On the second, the probe must answer four questions, not one: the global row those events share fixes a **wire shape and not an effect**, and its members' outcomes are mutually incompatible (`UserPromptSubmit` erases the prompt, `Stop` *continues* the conversation, `PreCompact` cancels compaction, `PostToolUse` annotates and continues). That run went from rev 13 to rev 20 unbroken before the last two rounds closed it, and the shape repeats: rev 13 routed a `SessionStart` stop the reference's **third** global table forbids (`hooks.md:1009`); rev 16 found a flip list that pre-filled the answer its own probe exists to obtain; rev 17 found "`continue:false` **only**" written as a rank-1 *rule* when it was only a census of what reaches rank 1 today; and rev 20 found a seam list offering an option that failed rev 19's own just-written rule. Each round names the rule that was broken, so the count is checkable. **rev 23 closed the gates**: G2 and G6 took their documented weaker branches, G8 dropped the pre-execution validator, G7's artifact question resolved to the provenance table — and a probe of a real `claude` 2.1.251 (**docs/reference/hooks-probe-2.1.251.md**) settled four rows the reference could not. Both contested rows are now **measured**: `SessionStart` discards `continue:false` (narrow reading confirmed) and `PostToolUseFailure` **honors** `decision` as feedback-and-continue (narrow reading reversed, with a control run proving the mechanism is the recognized field and not echoed stdout). The same probe found agentao's `/bin/sh` hook baseline **conformant** (upstream runs `sh` and ignores an explicit `shell`), confirmed that a schema-invalid `updatedInput` is rejected upstream with the original never running, and captured six real stdin payloads that confirm §5.3's shape. **rev 24 closed the remaining five**, so all ten are shut: four by maintainer decision, six table rows measured against a real `claude` 2.1.251 (one of them only partly), the rest by taking the plan's own proposals at implementation — with **three deviations from the plan's text** recorded in §0 (G9's groups run sequentially rather than concurrently, the shape detector gained a fourth `undetermined` value, and §5.1's universal matrix gained a `stopReason` column). Code findings from the **pre-implementation** tree (`main@10b5fb8`), each with what `18fb628` did to it — the citations are evidence for the gap, not a map of the fix. **Still true:** `StopHookResult.system_message` is written on the `agentao-v1` path (`_output_parsing.py:285`) and **nothing reads it** — the profile path routes `systemMessage` to `user_notices` instead, so the dead field survives on the frozen contract. **Closed:** the `${CLAUDE_PLUGIN_ROOT}` placeholder is written `env=build_child_env(placeholders)` (`_dispatcher.py:608`), and the hazard — any other spelling **silently deletes the credential scrub** — stands for whoever adds the next exec site; a `PostToolUse` stop no longer dies in the worker three frames below anything that can act on it, it rides home on `ToolExecutionResult.hook_stop_reason` and is arbitrated in **plan order** on `ToolRunner.last_hook_stop`; and the matcher is fixed *and* the finding corrected — a string matcher really is not `_glob_match`, but the probe showed upstream **anchors** rather than searching, so the profile branch is `re.fullmatch` with `*` special-cased (`_matchers.py:67`) while `_glob_match` keeps serving `toolName` and `agentao-v1`. **Moot:** the `HookOutcome` collision with `runtime/chat_loop/_outcomes.py:13` was avoided by naming — the type shipped as `ParsedHookOutput`. A `PostToolUse` `decision:"block"` is still **not** a stop: it *"adds the `reason` next to the tool result. Claude still sees the original output"*, and the turn continues. `.zh.md` twin.
- **compaction-orchestration-plan** — *rev 14 after twelve maintainer reviews; **implemented 2026-08-24**, unreleased — all six PRs merged in dependency order, #187 → #188 → #189 → #190 → #191 → #192. Where the PR descriptions and this document differ, the PRs are what is on `main`* — six PRs converging the five compaction entry points (microcompaction, threshold, the two API-overflow rungs, manual `/compact`) behind one `CompactionCoordinator`; closes the two P1s from **pi-mono-compaction-vs-agentao** (`PreCompact` is notify-only; the circuit breaker has no reset path). Sequence is **PR-1 → PR-3 → PR-2 → PR-4** (a dependency order, not a schedule). This document is the `PRECOMPACT_GATE_PLAN.md` placeholder those docs named but never wrote. `.zh.md` twin.
- **cli-host-agent-factory** — *shipped* — instance-scoped `agent_factory` on `AgentaoCLI` / `cli.main()` so thin Python hosts reach `extra_tools` and the existing runtime construction contract without monkey-patching; validates the returned runtime (#132, 2026-07-19).
- **acp-g4-plan-modes-commands** — *proposal* — surface plan / modes / commands as ACP `session/update` (chat-target now-work).
- **deepchat-acp-patch-revision** — *impl in progress* — DeepChat/TensorChat ACP integration; what upstreams vs stays in the fork.
- **host-fs-policy** — *proposal* — path-domain write boundary over the single fs chokepoint (incl. shell).
- **host-tool-allowlist** — *draft, converged* — `enabled_tools` additive allowlist.
- **mcp-tool-list-pagination** — *shipped 2026-08-03 (2 review rounds)* — agentao issued one `tools/list` per server and never read the cursor, so a paginating server's tools past page 1 were **silently** invisible (`grep next_cursor` → zero matches repo-wide). Borrows codex's bounds (#36039/#35724) with the loop. §4 is load-bearing: `params=` is the one call spelling working on all three CI SDK cells incl. the 1.26.0 floor — **probed, not recalled** — while the `nextCursor`/`next_cursor` read needs the existing `_compat.field`. Round 1 fixed three correctness defects (`cursor is not None` — `""` is a legal cursor; item cap gates `tools.extend`; cursor cap counts UTF-8 bytes) and closed D1 to **one rule — fail that server** — the isolation being `connect()`'s own non-re-raising catch at `client.py:310-347`, **not** `_connect_one`, which never fires here. Round 2 fixed four *prose* inaccuracies worth not re-deriving: the `""` cursor causes a wasted request + **wrong verdict**, not a hang (the repeated-cursor guard does catch it); the item cap is a **catalog-accumulation** bound, not DoS/wire, since the SDK parses before `_list_all_tools` sees anything; the single-page-overflow test **cannot** pin check-before-`extend` and must not pretend to; and **`permissions.json` does cover MCP tools** (`decide_detail` runs before `requires_confirmation`) — only `enabled_tools` and the name prefix are non-boundaries. A round-3 xhigh review then corrected the precedence once more: the **read-only mode preset short-circuits ahead of the engine** (`tool_planning.py:381-389`), so "the engine sees every call" is true only past that preset — the same overstatement had propagated into CLAUDE.md and both language docs (2026-08-03).
- **metacognitive-boundary** — *impl deferred* — metacognitive boundary as a host-injectable protocol (schema + default + override).
- **powershell-support-spec** *(zh only)* — ***rev 48** (2026-09-05) — PR-1 through PR-6 implemented, merged and green on the Windows job; the native identity oracle answers 20/20 and NAME-02's command table is measured, so PR-7 (the flip) is no longer blocked on a measurement — only on q14 and on G09's three preconditions* — PowerShell on Windows, blueprinted on codex then past it. The rev 24 monolith was split on 2026-09-03 into a file set because four review rounds running had the same headline — a rule reworded in one place and quoted stale in five others. **Every rule is defined exactly once, under a stable ID** (`TOOL` `SPEC` `LAUNCH` `ENV` `IMG` `LADDER` `CFG` `TOK` `LOWER` `WRAP` `NAME` `EFF` `CMD` `BASH`), and everything else references the ID: **powershell-support-implementation** (PR ladder, module ownership, six decision gates), **powershell-support-gates** (a traceability matrix, one row per case, six fixed columns), `docs/reference/powershell-support-evidence` (every `file:line`, measurement and upstream fetch — the only file with citations) and **powershell-support-review-log** (40 rounds, 301 findings, 29 method rules). Seventeen consecutive security reviews of the split spec (rev 26–31, 33–41, 43–44; rev 32 and rev 42 are structure only), each finding the previous one's fix: rev 26 the missing `git_bash` grammar gate and the closed set's real boundary (which program *starts*); rev 27 the environment, the decided spec bound to the launch, the interpreter's start directory; rev 28 that closed environment listing config and trust roots back in for want of a written *membership criterion*; rev 29 five of rev 28's new interfaces not closing (an identity→rung→identity cycle, no target-side pinned environment, a launcher identity that could not be an attested image); and **rev 30 the end-to-end run**: policy-off rungs could not launch at all (a `LegacyLaunch` variant now carries today's behaviour verbatim), the attested set did not necessarily contain the launcher being started (a `git status` body attested git but not the shell, so the executor's new MUST would have refused every external-only call), the child environment was still computed from implicit sources and twice, `PinnedEnv` typed drive letters and root-relative paths as absolute paths with no validation point, three identity reads went unchecked, per-string length limits shared one reason with the whole request, and the prose still asserted the answer a probe was supposed to measure. rev 31 then found six more in rev 30's own fixes — a `LegacyLaunch` variant that still inherited the attestation fields it was introduced to shed, an environment built before the fail-closed checks that should have rejected the spec, an oracle answering subject-dependent questions without a subject, a platform "snapshot" read once per candidate, Windows command-line limits compared against argv plus environment, and a freeze that was only skin deep. `scripts/check_design_set.py` holds the set together in CI. English versions are generated once before implementation starts. **rev 32 and rev 42 are structure only.** **rev 32:** §3–§5 moved out of the markdown into `powershell-support-contracts.py`, a stdlib-only module the design-set test holds to `mypy --strict` (the rev 29–31 headline defects — an `Optional` launcher dereferenced, a frozen record assigned, an `AbsPath` handed to a `ResolvedImage` slot — are exactly what it reports for free); the 11 rule rows over 900 B or three sentences were split verbatim into 32 sub-rules (`ENV-06a`…) with a mechanical equality check, and `check_design_set.py` gained anchor, cell-size and `--changed-since` review-packet checks. Spec 108 → 76 KB. **rev 33 is the seventh review, and every finding sits on the same seam: how a decided thing reaches execution.** The P0 — the decision bound the spec object but not the body or the working directory, because `launch()` accepted its own copy of both, so deciding `Get-Date` and then launching different text through the same plan was a legal call; the decided body, cwd, environment and attested set now freeze into one record the launch signature has no way to bypass. Three P1s: background execution was a second launch path the model could pick with one flag (`run_background` builds its own `shell=True` `Popen`), so an implementation could decide as PowerShell and launch as cmd with every gate still green; the Git Bash grammar gate rejected command and process substitution but not the unquoted expansions that change argv, and `git {-c,core.fsmonitor=./evil} status` reads to the effect table as one ordinary literal word; and reparse resolution returned `AbsPath | None`, which spends one value on both "not a reparse point" and "could not resolve" — the caller filtered the `None` away, and a junction cycle recursed without a bound. Two P2s: `filesystem_is_local` was false on every constructor path, so the local spec a gate calls for could not be built; and the prelude carried two contradicting MUSTs about its own ordering plus a "does not perturb the body's semantics" it violates in every clause. **rev 34 is the eighth review, and its seams are all "an enumerated value the contract never constructs":** the P0 was that `select_rung` could not build the two policy-off rungs at all — the ladder is `pwsh → powershell → cmd`, so a POSIX target found nothing and pre-flip Windows had no constructor, and LADDER-03 turns an exhausted ladder into a DENY on every shell call, while G10-03 had asserted the `LegacyLaunch` outcome all along. Seven P1s followed, among them both re-entry points in `analyse_body` with no depth bound (the same round had just added one to the reparse walk, for the same reason), an `env_passthrough` element type that let one `["*"]` restore the whole inherited environment, a `launch()` that took its own oracle when SPEC-05 binds an oracle to a subject, and a length gate whose `if Windows … elif POSIX` had no `else`. **rev 35 is the ninth, and every finding sits one layer below the gates: a field the contract registers but nothing reads, or a signature that promises a rule it cannot answer.** `predicate_positions` was EFF-06's only foothold and had no reader at all, so `Remove-Item $flags C:\` matched no effect trigger and read as inert — the gate for it (G05-02) had existed for eight rounds. `caller_scope` was read on one of the two branches that can produce its effect, which is the wrong one for `iex`. Four early returns in `decide()` left the *previous* call's decided body, working directory and environment in place for `launch()` to consume. `filtered_path` promised IMG-01 filtering with no oracle in its parameter list, and `resolve` promised the filtered PATH without receiving it. The other three are in `check_design_set.py` itself: the anchor check scanned the whole contract source, so any prose mention kept it green once the real `# FAM-NN` was deleted; the review packet's gate lookup did not follow the parent lean that `coverage` allows; and an unresolvable `--changed-since` baseline reported every rule as newly added and exited 0. Method rule 18 records the shape: **a registered field needs a reader, and a signature that names a rule needs the inputs to answer it — and no mechanical check in this set can see either gap.** **rev 36 is the tenth, and it splits in half.** Two P1s are completeness nobody counts at run time: `PinnedEnv.shapes_ok` checked field shapes and cross-platform nulls but never presence, so a Windows oracle answering `system_root=None` produced a child environment silently missing `SystemRoot` — while ENV-06a's own words are that an environment short one system root is not a safer environment; and SPEC-05c says an oracle *missing any method* leaves the rung unattested, which a `Protocol` cannot enforce at all, so a launcher could be selected and a command approved before `launch()` raised `AttributeError` instead of denying. The other four are one mistake: rev 35 fixed the parent/sub-rule lean in two places and left the same relation hard-coded to the exact ID in three sibling consumers, so the checker accepted what its own review packet reported as a seam. Method rule 11 gains a clause — the grep list is not only documents — and method rule 19 records the first half: **whenever the spec says complete, closed, or "missing any one is a rejection", there has to be an explicit list and an explicit count, taken before anything is decided on it.** **rev 37 is the eleventh, and three of its five findings land on rules written the round before, in the very function that was being edited when they were written.** The heaviest is a regression of my own: putting the oracle-completeness check at the top of `select_rung` to satisfy SPEC-05c denied every shell call on a default executor, because SPEC-05c's own last clause is that policy-off rungs keep running and those two rungs sit below that gate and never consult an oracle — the same victim as the rev 34 P0, in the same place, a second time. `PinnedEnv.home`, `temp` and `tmp` were left out of rev 36's new mandatory list because they are annotated non-optional, which a dataclass does not enforce on an oracle-supplied record. The review packet's anchor flag mirrored one of the three conditions the checker applies and not the other two, which falsely flagged every `SUB` rule against a baseline predating the subagent plan. The remaining two: `policy_of(spec)` was an implicit source for the allowlist, so two configurations differing by one pin produced the same fingerprint — the allowlist now freezes into the spec and into that fingerprint (IMG-03a) — and `git show` decoded with the host locale, which cannot read these Chinese documents on Windows. Method rule 20 records the recurring one: **an exception clause is part of the rule; when you add a fail-closed gate, count the returns it just made unreachable.** **rev 38, the twelfth, found two.** On a POSIX target an explicit `shell.path` passes the trust-root chain, the project-root check and the identity read, and then derives a policy-off rung whose constructor has nowhere to put it — so `/bin/bash` and `/bin/zsh` produced the same spec and the same fingerprint and the launch used today's shell instead, silently discarding what the user named. The selected executable now freezes into the spec and its fingerprint (CFG-02c). The second is the same failure mode for the third round running: the review packet restated one of the checker's acceptance routes and not the other, this time missing that a gate named in a rule's own row counts — which reported every `SUB` rule as ungated on a change the checker fully accepted. Adding the missing condition would only have added another copy to keep in sync, so both questions became single functions that `check_set` and the packet call, verified byte-identical on two review packets. Method rule 12 grows from rules to predicates, and method rule 18 gains its dual: **a value that was checked is not thereby a value that was kept.** **rev 39, the thirteenth, lands the first finding on the half of the command line that is not the body.** The working-directory encoder doubled the ASCII apostrophe, but PowerShell's tokenizer recognises five single-quote delimiters; a directory named with a typographic apostrophe closes the generated `Set-Location` literal and appends commands to the prelude. No gate could catch it, because every rule in the set asks how an untrusted body reaches a decision, and the prelude is text agentao assembles itself — the floor never reads it. The four non-ASCII quotes are now refused outright (LAUNCH-09e), since whether doubling escapes them is unmeasured and guessing wrong is an injection while refusing is one denied call. The second finding is method rule 20's third entry, and this time the second version of the same repair: requiring `target_filesystem_is_local()` for rung selection emptied the ladder for an executor missing only that method, although SPEC-04a says an unanswerable locality reads as false. Method rule 21 records the new class: **the floor scans the body; every other string agentao puts on the command line is governed by no grammar gate at all.** **rev 40, the fourteenth, found two, and both are something filed under the wrong heading.** `PUBLIC` (`C:\Users\Public`) sat in the pinned group that must pass IMG-01, although it is a shared user-data directory writable by design and nothing in the spec loads or reads configuration from it — and IMG-06a's directory test includes `FILE_ADD_FILE`, so that one key rejects every policy-enabled rung and denies every shell call after the flip. The root cause is that the split between the two pinned groups never had a written membership criterion, so the key was filed by how its name reads; ENV-06g writes the criterion down. The same criterion keeps `C:\ProgramData` in the strict group, and whether *its* stock ACL rejects every rung too is unknown and not guessed: q14 and probe G23-12 settle it before PR-4. The second is my own rev-38 repair overreaching — expressing "a changed sub-rule counts for a gate naming its parent" by folding the parent into the changed set made every *other* child look changed and swallowed a real warning. Method rule 15 now covers partitions as well as sets, and method rule 12 gains the matching half: **state a relation as a predicate; widening a set to express it imposes that relation on every other reader.** **rev 41, the fifteenth, found three: a dead configuration value, a false positive, and an exception standing in for a verdict.** A `PublisherTrust` entry in the allowlist had no reader anywhere — the lookup selects hash pins only and the signature question never received a signer — so naming a trusted publisher and naming none produced identical decisions, although IMG-03 defines it as one of the allowlist's two forms; the oracle gained the missing question and IMG-03b fixes the single consumption point. The trusted-root walk mixed "ancestors already permission-checked" into "entries of the current reparse traversal", so a trusted junction pointing at its own parent was rejected as a cycle even though it resolves and passes every check. And a lone surrogate — which survives JSON decoding into a Python string — made the length gates raise `UnicodeEncodeError` before any analysis ran. Method rule 22 records that last one: **an exception thrown inside the floor is not a verdict**; it carries no reason, is not covered by the rule that a floor DENY cannot be masked, and upstream may hand it back to the model as a retryable tool error. All three fixes were exercised directly against the contract module. **rev 42 answers a pattern rather than a finding.** Across fifteen reviews about 10 of the 281 findings were introduced by the previous round's fix, and they split in two: six were one rule with several consumers where only one was updated — which no method rule stopped, and extracting the shared predicate did — while the other four were an earlier fix silently undone inside the contract module, in four functions nothing tested. The module holds 46 functions with real bodies against 25 `Unspecified` seams and 22 `...` Protocol methods, both signature-only, and the only thing ever run over it was `mypy --strict`, which checks types and not behaviour. `tests/test_powershell_contracts.py` now pins that behaviour in 61 tests: bodies only, seams stubbed and never asserted on, each test naming the rule it holds. Twelve fixes were reverted one at a time to confirm the matching test goes red. The limit is stated rather than glossed: a regression suite stops fixes from undoing each other, and invents none of the novel findings — the typographic-quote injection and the dead publisher entry came from a reviewer thinking of a case nobody had. **rev 43 is the sixteenth review, run by an external code-review agent, and its two hardest findings are the two it handed back rather than fix.** In the fixed dialect table only the `cmd` row never reads the target platform, so an explicit `dialect: cmd` on a POSIX target passes the whole image pipeline and derives a policy-enabled `cmd` spec — for which none of the three length measurements applies, so every call is denied as oversize, a length reason for a platform error. It is the rev 34 headline a second time: an enumerated value the contract should never construct. The other is the reverse of the reviewer's advice — `canonicalize` has no caller and looked like the dead configuration rev 41 removed, but its caller is the PATH-filter seam, whose containment test is documented to take already-canonical paths while PATH entries are raw environment strings; deleting the declaration would have turned an unimplemented requirement into a nonexistent one, and that requirement is what stops `..` from walking a writable directory back onto the child's PATH. Two more came from auditing the agent's own fixes: narrowing the environment-key match from a substring to a whole identifier kept a full case fold, and the contract has 47 lowercase `path` parameters, so the hole simply moved one level down; and a heading had been duplicated in the working tree where the repeated-phrase check could not see it, needing 30 characters against a shorter line. Method rules 23 and 24 record the pair: **re-measure the population a narrowed check was failing on, not the one example in the finding**, and **a declaration with no caller is not dead if a seam still owes it**. **rev 44, the seventeenth, found three, and all three are a state with nowhere to land.** The strict type check reported success whenever mypy exited nonzero with nothing on either stream, which is exactly how it exits when the OOM reaper kills it or a plugin crashes, so the one gate whose absence nothing else would notice could silently not run. `SPEC-04a` says both an absent locality method and an unanswerable one read as false, but the signature returned a plain `bool`, so the second state could only be signalled by raising, and that exception escapes rung selection before either policy-off rung has been chosen; the signature now carries `None` and `mypy --strict` rejects the old expression on its own. And the parent/child lean was written on one of `gates_for`'s two coverage routes, so whether a sub-rule counted as gated depended on which *kind* of gate its parent happened to have — the `SUB`, `MCP` and `ENG` families are all covered by bulleted definitions, so none of them could be given a sub-rule at all. That is the same shape for the fifth time, and the first time it was inside the single home rather than between two copies of it. Method rule 25: **a child process's silence is not a pass**; rule 12 gains the matching half: **one definition point is not one application point**. **rev 45 is not a review round but the implementation of PR-4, and its seven findings all come from writing a rule down as code and watching it fail to close.** The heaviest is a name collision: two different tables are both called 实测表 in the prose — the edition table `derive_rung` reads, whose absence refuses the source, and NAME-02's measured command table, whose absence makes bare words opaque while the rung stands — and the contract had applied the first one's consequence to the second, which would have dropped Windows to `cmd` and its coarser floor over a missing *name* table. Three signatures could not answer the rule they named: the trusted-root walk and both containment tests compare paths by the target's rules with no target in their parameters, and re-asking the oracle would break the single-call snapshot a gate asserts; and `prelude_for(identity)` cannot render LAUNCH-05's prelude at all, because `<W>` is per call while the identity is per rung. The everyday toolchain was registered under the POSIX dialect only, so `git status` would have been opaque on both Windows rungs that a gate says must allow it — the application entries are now defined once and instantiated per dialect, since three hand-kept copies drift silently. The fingerprint's canonical encoding had no case for a frozenset, which every policy-on spec now contains, so constructing one raised. And one was self-inflicted and caught by its own test: looking an allowlist pin up by the target's loose path rules while confirming it by string equality turned "this image is unpinned" into "this image is untrusted". Method rule 26: **when the same pair of values is compared in two places, both places must use the same rule** — a loose lookup confirmed strictly is not a belt and braces, it collapses two different outcomes onto one path, and the gap it hides needs a name (here, q15: nothing canonicalises a user-written allowlist path). **The Windows job then paid for itself twice over.** Its first run failed 155 tests, and two were product defects POSIX had been absorbing: writes translated `\n` to `os.linesep` while the edit tool read bytes, so every edit of a CRLF file doubled its carriage returns; and the no-home fallback tested POSIX mode bits Windows does not set, handing out a fresh state directory on every call. Its second run found two more of the same kind — every memory read leaked a SQLite connection, because `with conn` commits without closing, and two session saves inside one clock tick silently overwrote each other, because the filename is the clock and nothing checked whether it was taken. Method rules 27 and 28 come from the same place: 27 says name the test that goes red when you revert a fix, and 28 is its boundary — the CRLF fix had such a test, and still missed the third writer in its own function, because the platform where the defect happens is not the platform the suite usually runs on. **rev 47 is a measurement, not a review.** IMG-06a applied one directory mask to the two roles a directory plays on a chain, and a stock `C:\` grants every standard user `FILE_ADD_SUBDIRECTORY` and none of the destructive rights — the volume root is on every chain, so IMG-01 was false for every path, every subject, every stock Windows, which LADDER-03 turns into a denial of every shell call after the flip. Seventeen documentation reviews read that rule word by word and it reads correctly; what was wrong was its unstated assumption about factory ACLs. **rev 48 closes the last two open gates without a probe.** q11 asked which `rebinds_caller` scope cmd's `call` and `start` carry, and neither reaches the point where the flag is read, so the answer cannot change a verdict — EFF-07 already said both were unreachable and drew the conclusion for only one of them. And LADDER-04 gated the flip on "the lowering rate in three buckets, accepted", where the phrase occurs four times in the file set, every one a restatement of itself: no enumeration, no threshold, no corpus, no owner, nothing ever measured. It is replaced by three preconditions that can go red and a distribution collected after shipping, since the design already lists an unusable opaque bucket as a thing that would change it. Writing the first of those three as a test then found two more defects: q9's inert set named `python -c` and `node -e`, which EFF-01's own definition forbids and a gate had pinned opaque for rounds, and EFF-07's closing clause had been abbreviated at the split from three named shells into "any interpreter fed a script path", which reads wider than the boundary §1 draws. Method rule 29: **a gate nobody can fail is not a gate** — say what makes it go red, and when the quantity you want only exists after shipping, gate on the instrument and the way back instead of on the number.
- **subagent-runtime-safety-plan** *(zh only)* — **the former PR-0, independent of PowerShell: its engine half is a live, measured defect and should be fixed now; its MCP half is not authorized** — sub-agents are constructed with no `permission_engine=`, the runner copies `None` into its planner, and the wrapper's later assignment writes an attribute the planner never reads, so a sub-agent decides `rm -rf /` as ASK and three transports auto-approve. The fix is an internal factory sharing the parent's engine/fs/shell by identity, a registry rebuilt by origin, an MCP owner thread with a non-owning scoped view and per-call cancellation context, and an engine behind one writer lock whose decisions carry their snapshot (`SUB-*`, `MCP-*`, `ENG-*`).
- **powershell-support-plan** — *frozen at rev 24 (commit `e01293f`), superseded by the file set above; not maintained, kept only until the split spec has had its own full security review.*
- **permission-hardening-plan** — *impl plan, rev 3* — shell-pattern hardline scanner hardening.
- **tool-search** — *draft, deferred* — deferred-loading tool discovery. **Two reference implementations** as of 2026-08-03: codex (heavy — `ToolExposure` enum + core BM25 `tool_search`) and pi-mono (light — a 39-line stateless `splitDeferredTools`, contract is one `addedToolNames` field, search itself is a *userland example extension*). pi-mono adds activation model **(c) transcript-carried**, which keeps the tool prefix byte-stable but needs a provider-native load point agentao's chat-completions path lacks — so it **does not lower agentao's cost**, and its driver is provider-led, not tool-list bloat, so it is **not** a second instance of the trigger. Decision unchanged.

## Landed contracts · 已落地契约

Shipped behavior — read as reference for what exists today.

- **host-tool-injection** — `extra_tools` / `disable_tools` (v1 landed).
- **runtime-tool-injection** — `add_tool` / `remove_tool` (v1 landed).
- **host-llm-extra-params** — host LLM request passthrough `extra_body` (v1).
- **run-spec-parameters** — `agentao run` spec parameters & instructions (shipped 2026-05-25).
- **mcp-streamable-http** — MCP Streamable HTTP transport; bare `url` now defaults to it (**breaking**, SSE is opt-in via `type: "sse"`). Shipped 0.4.14, 2026-07-02. **§5.1/§5.3 carry mcp-2.0 update blocks** (0.4.17): the stream tuple lost its third element and the SDK moved to `httpx2` — an arity that no signature or field name exposes, only the `yield`.
- **lint-gate** — the CI `ruff check .` gate: which rules, and the measurement behind each inclusion *and exclusion* (2026-08-03). Two non-obvious records: `F821` is inert in star-import modules unless `F405` is also selected, and `F401` is undecidable inside `agentao/` because a name re-exported for embedders looks identical to dead code from in-repo.

## Review & decision records · 评审与决策记录 *(the "backlog" class)*

Retrospective analyses — competitive/borrow reviews, conformance gap reviews, decision records. Evidence-before-recommendation; **gap ≠ need**. Not active build specs.

- **hooks-three-way-claude-codex-agentao** — **analysis only, nothing authorized for implementation *from this document***; §1 is a priority ordering of findings, not a schedule. Its nine deviations were closed by **hooks-claude-contract-conformance-plan**, merged 2026-08-30 (PR #199 / `18fb628`); this doc stays the **evidence**, measured against `main@10b5fb8` and not re-derived after the fix. **rev 5 after four maintainer reviews — rev 2 fixed six errors on the agentao side, rev 3 four on the codex and reference sides, rev 4 three on §5.9's scope plus one on §5.10, rev 5 the quantifier below; read §9.2 before citing any count from this doc.** Three-way **hook-contract** comparison (input *and* output, widened in rev 2) of the Claude Code hooks reference vs codex's `codex-rs/hooks` vs agentao's plugin hooks. **§0 is load-bearing** — the reference documents **31** events, codex 11, agentao 8, and that spread is *not* a defect on either peer; the comparable question is whether the events all three *do* implement speak the same contract. Nine places where agentao's does not, **ten of the doc's assertions measured** through the real dispatcher and a real subprocess — nine behavioural, plus §5.9's envelope-and-field sweep, which rev 4 drives across **all eight** dispatch paths after rev 3 earned the *(measured)* marker on a two-event sample (one probe, reproduced verbatim in §9.1 and re-run against the published text). **Three P1s:** the **stdin contract diverges on all 8 events** — three layers, the envelope (`{event,data}` camelCase on 6 of 8, so the reference's own `jq -r '.tool_input.command'` example returns `null`, `models.py:230`), event-specific fields on 7 of 8 (`prompt`→`userMessage`, `source`/`reason` absent, `tool_response`→`toolOutput` with an object→string type change, `tool_use_id` nowhere), and common fields or their values on **8 of 8** — `PreCompact` clears the first two and still ships a hardcoded `transcript_path: None` plus a `permission_mode` the reference does not put on that event, so no event matches end to end (§5.9, the largest single portability break, absent from rev 1); `UserPromptSubmit` silently drops all four documented output channels — `decision:"block"`+`reason`, `hookSpecificOutput.additionalContext`, `continue:false`, exit 2 — honoring only agentao's own spellings (`_output_parsing.py:65,77,90`), falsifying the module's stated goal at `_alias.py:5`; and `systemMessage` is routed into the **model** context channel rather than the user-warning channel, persisted to `agent.messages` on one Stop site (`_runner.py:1042-1053`) and dropped on the other three (`_runner.py:222,228,236`). Then P2/P3: **`Stop` `hSO.additionalContext` is parsed but never sets `force_continue`** so the turn ends where the reference continues it (§5.8 — rev 1 marked this ✅ on the strength of the parse alone; *the parse is not the contract*), no output bound at all, `PreToolUse` `additionalContext` parsed-then-logged (`tool_runner.py:308`), `continue:false` and exit 2 both `Stop`-only, no `${CLAUDE_PLUGIN_ROOT}`, and `suppressOutput` — documented inert by the reference — actually suppressing the `<stop-hook>` echo, so a hook that sets it loses its `systemMessage`/`additionalContext` display (§5.10, kept a note only because that is §5.2/§5.8's breakage reached through a second field). **§4 is the one to read before filing a `PreCompact` bug**: the reference blocks on exit 2 or top-level `decision:"block"`, codex uses `continue:false` (the field the reference says is *discarded* for that event — and it surfaces the discarded `systemMessage` there too), agentao uses an invented `hookSpecificOutput.compactionDecision:"cancel"` — **three spellings, none conformant**; refines and re-verifies the `PreCompact` note in **codex-compaction-vs-agentao**. **§6 is the calibration and must not be skipped**: codex deviates in at least **nine** places of its own — a **selected** list, not an exhaustive count — rejecting `permissionDecision:"ask"` outright, **rejecting `continue:false` on `PreToolUse`/`PermissionRequest`** (rev 1 wrongly claimed codex honored it universally; the per-event table is §5.5), **rejecting `suppressOutput` as unsupported on three events** where the reference calls it inert, no host-side cap on consecutive Stop blocks, and exit 2 honored on `PermissionRequest` where the reference says it is not. **§7 lists five axes where agentao leads** — provider credentials scrubbed from the hook child (codex's hooks inherit the API key), a Stop reentry cap, `ask` support, `PostToolUseFailure` (codex has no such event, which is why the union of implemented events is **12** and the events neither peer implements is **19**, not 20), and runnable `prompt` handlers. §8 is parity — **do not re-flag it**. **§9.2 carries four reusable method records**: *a model-summarized spec is not a spec* (the first summarizing fetch got `continue`/`stopReason` nesting and the `permissionDecision` values wrong, so every reference claim was re-derived from raw markdown — Mintlify serves it at the same URL with a `.md` suffix); *count the units the claim is about, not a proxy for them* (rev 1 counted codex's exit-2 support by parser files, 5, rather than events, 6, because `Stop`/`SubagentStop` share a parser); *the reference's global tables are overridden per event, and the per-event section is the authority* — which held up across two reviews, since the rev-3 fix over-swung into claiming `model` is owed to nothing agentao implements when agentao implements `SessionStart`, exactly where `model` lives; and *the probe has to cover the population the claim quantifies over* (two reviews to apply: rev 2 printed a builder's return value, rev 3 sampled two of eight events and still generalized). Anchors: reference fetched 2026-08-26, codex `0d9bb6c34c`, agentao `main@10b5fb8`; en+zh twins, 74 identical citations / 135 line resolutions checked mechanically against source. Secondary codex anchor added in rev 5: OpenAI's own hooks reference at `developers.openai.com/codex/hooks`, which corroborates the source-derived codex claims and is silent on `PermissionRequest` exit 2 (2026-08-26). **One dated correction, 2026-08-28 (body otherwise rev 5):** the `UserPromptSubmit` plain-text-stdout row said codex's `Failed` on `{`/`[`-leading invalid JSON diverges from a reference that "falls back to plain text" — re-fetching shows that fallback is the **pre-v2.1.248** behavior, so codex is conformant there and **agentao's ✅ is the divergence**; the corrected rule lives in the conformance plan's §4.2 (rev 7). No other row was re-verified against the new fetch.
- **builtin-tools-four-way-codex-gemini-pi-agentao** — **analysis only, nothing authorized for implementation**; §1 is a priority ordering of findings, not a schedule. **rev 17 after sixteen maintainer reviews — 8, then 5 *in round 1's corrections*, 5, 4, 3, 3, 1, 1, 1, 1, 2. Every round found errors inside the previous round's fix; treat any claim not carried by the current text as withdrawn. Every round corrected the previous round's fix, so cite nothing from rev 1–4 without checking the revision-history table.** Which tools each harness ships **in-tree and model-visible**, and what decides whether one reaches the model on a given turn — codex `b7cd519c76` (2026-08-31), gemini-cli `0bd1d4397` (2026-08-28), pi-mono `853a80d26` (2026-08-28), agentao `main@afda2ea` (2026-08-31), all four read from a local worktree, no vendor docs as a primary source. **§4's last column is the one to read if you care about runtime injection**: "schema projected" is a *when*, and the three non-codex harnesses answer it differently — **gemini-cli is the loosest**, refreshing only at `startChat`, an explicit no-arg `setTools()`, or a model switch (the per-request call short-circuits on an unchanged model, `core/src/core/client.ts:311-313`); pi-mono defers to the next agent turn (`core/agent-session.ts:965-971`); and agentao snapshots once per `chat()` **above** the inner tool-call loop (`runtime/chat_loop/_runner.py:348`), so a mid-turn `add_tool` is invisible for the rest of that turn however many iterations it runs (`agent.py:906-914`). **§0 is load-bearing**: tool *count* is not a quality axis — codex carries ~50 names, pi-mono ships 8 and shows the model **4**, and pi-mono's 4 is a deliberate, *compensated* withholding (it wrote `grep` / `find` / `ls`, keeps them out of the default set, and substitutes shell guidance in the prompt, `core/system-prompt.ts:99-111`) — so reading the spread as a deficit gets the comparison backwards. **Why** it withholds is not in the source: rev 3 called it a context-cost bet and paired it with codex as two independent data points; rev 4 marks that **inference, unmeasured**. **The three P1s are all rev-1 errors this revision withdraws, and each one killed a conclusion:** (a) **codex does not have "0 file tools"** — `apply_patch` is a model-visible workspace-write tool whose handler routes patches to the environment filesystem and derives write permission **per target path** (`core/src/tools/handlers/apply_patch.rs:73,236-270`), which is closer to gemini-cli's `TOOLS_REQUIRING_NARROWING` than to "the sandbox is the only boundary"; the inferences that codex's permission unit *cannot* be the tool and that a **3:1 vote** puts agentao in the majority are both **withdrawn**. rev 2 then replaced that with "codex has zero **read** tools", which **rev 3 also withdraws**: `view_image` takes a local path, resolves it against the environment cwd and reads it through the sandbox filesystem (`handlers/view_image_spec.rs:19`, `handlers/view_image.rs:150-175`), Stable and default-on (`features/src/lib.rs:889-893`). The column is now explicitly *general text/source* reads, and the claim that survives is **"no general reader", not "no reader"** (§3). (b) **Finding 3's peer evidence was the wrong constant** — `PLAN_MODE_TOOLS` (`tool-names.ts:283`) has **no runtime consumer anywhere**; the live policy is `read-only.toml:30-55` / `plan.toml`, and it puts `activate_skill` on **ask** (`plan.toml:106-111`), so gemini-cli **agrees** with agentao there rather than contradicting it. "None of the three touches the workspace" was also false — `save_memory` persists through `MemoryManager.upsert` (`memory/manager.py:80`) into project or user SQLite. The observation survives on corrected evidence and is narrower: `read-only.toml` explicitly allows `tracker_*`, `update_topic` and `complete_task` under the comment *"safe as they only modify internal state"*, and agentao's `todo_write` is that class and is denied — plus gemini-cli's **ask** is a third position agentao's binary `is_read_only` cannot express (§5). (c) **"the engine runs for every tool call" is false** — the read-only preset returns `DENY` *above* the engine (`tool_planning.py:487-495`) — **and gemini-cli does run a uniform pass** (`scheduler.ts:648-652`), so §8's first lead is **withdrawn** and §8 is now **three** leads, not four; on that axis agentao and gemini-cli are level, codex resolves before the tool layer, pi-mono has no engine. **Four P2s, each a qualifier rev 1 omitted:** default counts need a host qualifier — `bg_store` defaults to `None` in the constructor (`agent.py:148`) and only `build_from_environment()` wires one (`embedding/factory.py:231`), so it is **11 embedded / 13 CLI-factory**, +2 each with the `[web]` extra; `cli_help` has no in-tree instantiation but both classes are public exports registerable via `extra_tools=` (`agent.py:194`), so "dead class" overstated it — it is a stale comment plus two never-defaulted exports, and deleting them is an API change; gemini-cli **does** gate on a model-name heuristic (`isGemini2Model`, `core/src/config/config.ts:1293-1297`), so Finding 1's axis is a **three-position spectrum** (harness-maintained compatibility fact — a `ModelInfo` catalog mixed with `provider.capabilities()` for codex, a bare **regex** for gemini-cli / a model-name heuristic / no model reading at all), not a binary — and rev 1's blanket "host config only" contradicted its own §2.3; and `get_internal_docs` **is** model-reachable through the `cli_help` subagent (`cli-help-agent.ts:89`), so §10 could not list it as unreachable while §6 counted the sub-agent-scoped `complete_task` as in scope. **P3:** rev 1 dated itself 2026-08-30 against two anchors committed on the 31st; commit dates are now carried inline so date and anchors can be checked against each other. **Surviving findings:** the model-capability gap (§4, tabled and demand-gated — the blocker is owning **some** continuously maintained compatibility fact, not a missing wire field and **not necessarily a catalogue**: codex mixes a `ModelInfo` catalog with `provider.capabilities()`, gemini-cli's is a regex (`config/models.ts:458`), so a regex or per-provider flag stays on the table — and **no precedent here rules on it**: two per-model borrows were declined, one on a defaulting hazard and one on an error-envelope hazard, neither on whether agentao should own model metadata); the `cli_help` comment drift; the read-only policy question; **and the peer defect that validates ours** — gemini-cli registers `list_background_processes` / `read_background_output` (`core/src/config/config.ts:4028-4037`) while both are absent from its own `ALL_BUILTIN_TOOL_NAMES`, so `isValidToolName()` returns false and `agentLoader.ts:103`'s zod `.refine()` **rejects any user-authored agent file naming either**, and its only test over that constant (`tool-names.test.ts:50`) checks the list against *itself* in the direction that cannot fail; agentao's `BUILTIN_TOOL_NAMES` has the reverse-direction test (`test_builtin_tool_names_constant_in_sync`), which is the peer evidence for keeping it. **§9 approves zero borrows** — codex's per-turn gating tabled; `get_context_remaining` / `new_context` declined with a *third* data point for `codex-compaction-vs-agentao`'s "mode default off" verdict; gemini-cli's plan-mode-as-tools declined 1/4; `TOOLS_REQUIRING_NARROWING` on **watch** only. **§10's three method notes are the transferable part**: resolve tool names to **string literals**, never the constant that holds them (gemini-cli's has drifted, so a list of constant names would have missed the peer defect entirely); **a tool class existing is not a tool being registered — and "registered" has more than one destination** (main registry vs sub-agent-scoped vs host-side-only, which is precisely what rev 1 collapsed); and, new in rev 2 from this doc's own citations, **a basename is not an address** — codex has four `apply_patch.rs`, pi-mono a second `config.ts`, agentao a second `registry.py`, and rev 1 pointed at the wrong file in each case with one also off by one. **rev 3's five, all against rev 2's own text:** [P1] the `view_image` correction above, which is **also a counter-example to §4** — `view_image` is registered without consulting `input_modalities` (`spec_plan.rs:1259`) and refuses at execution instead (`handlers/view_image.rs:97-105`), so codex is *"some tools narrowed by declared capability, others admitted and rejected at execution"*, a mixed strategy rather than a clean capability gate; [P1] **pi-mono has no per-tool permission boundary at all by default** — with no extension registering a `tool_call` handler `beforeToolCall` returns `undefined` (`core/agent-session.ts:489-492`) and the call executes unchanged (`agent-loop.ts:617-624`), and the permission gate ships as an *example* (`examples/extensions/permission-gate.ts:13`), so the row is now "no default policy; one uniform optional interception hook"; [P2] on `activate_skill` rev 2 said gemini-cli "agrees with agentao" **and** that its ask is "a third position" — contradictory, and the ask rule carries `interactive = true` (`plan.toml:110`) so a **non-interactive** run falls to the plan catch-all DENY (`plan.toml:76-80`); the behaviour is **interactive → ASK, non-interactive → DENY**, and the "binary cannot express ASK" point is now scoped to the `is_read_only` **field**, not to agentao's decision system (the engine's tier-2 ASK works normally); [P2] §8's **sub-agent-binding lead is withdrawn** — gemini-cli builds the sub-agent registry from the parent's `context.toolRegistry` (`local-executor.ts:190-200`) and `core/src/tools/tools.ts:480`'s `clone(messageBus)` is a shallow `Object.assign(Object.create(proto), this)` preserving config, directory and filesystem binding — parity, not a lead; [P2] §8's **plan-mode lead is restated** — rev 2's §9 said agentao keeps plan mode out of the tool surface, which contradicts its own §2.1 (`plan_save` / `plan_finalize` are model tools inside plan turns), so the real 1/4 difference is **mode entry/exit as a model tool** (`enter_plan_mode` / `exit_plan_mode`), which codex, pi-mono and agentao all lack. **§8 is now two leads**, down from rev 1's four. **rev 4's five, all against rev 3's text:** [P1] gemini-cli's **19–20 was the registration count, not the model-visible one** — `getFunctionDeclarations()` re-filters on every schema build (`core/src/tools/tool-registry.ts:601-624`): both MCP resource tools drop with no MCP server, and `enter_plan_mode` / `exit_plan_mode` are **mutually exclusive by mode**, so a bare session shows **16–17**. That also falsifies rev 3's "the model sees a constant set for the session" (a PLAN/YOLO transition calls `setTools()`, `core/src/config/config.ts:2810-2819`) and §8's "always-visible, policed at execution" — gemini-cli schema-filters the pair exactly as agentao does, so the surviving 3:1 difference is only that its model gets a posture-changing tool **at all**; [P1] codex's `ModelInfo` is **not model-self-declared** — it is a harness/backend-maintained catalog matched by **slug prefix** (`models-manager/src/manager.rs:617-631`) with a warned synthetic fallback for unknown slugs (`model_info.rs:142`), so §9's objection is **owning a per-model catalogue**, not "agentao's providers don't send the field" — the same objection that self-refuted `isRecoverableLength`, now correctly stated; [P2] the context-cost motive downgraded to inference (above); [P2] §8's typo-guard lead **merged two mechanisms** — `enabled_tools` validates against the **live registry ∪ `BUILTIN_TOOL_NAMES`** (`tooling/registry.py:195-205`) so it also accepts agent-path names, while `disable_tools` validates against the **static constant alone** (`agent.py:466-472`); the lead stands, split in two; [P3] §8 still read "the three below survive" after dropping to two. **rev 5's four:** [P1] **the precedent behind the catalogue verdict, wrong in rev 4 and wrong again in rev 5** — rev 4 cited `pi-mono-pull-review-2026-08-09`, which does not exist; rev 5 then defended the substance with `grep -r context_window agentao/` = 0, which is **the wrong field**. `maxTokens` is a requested *output* cap and agentao has the same knob (`llm/client.py:139,188,419-421`, ACP-mapped at `acp/session_set_model.py:10`). The real difference is the **default**: pi falls back to a per-model registry value (`ai/src/api/simple-options.ts:34`), agentao to a flat `65536` — so that borrow is portable when the host sets it per model and unsafe under the shipped default, a *defaulting* problem, not a catalogue one. `supportsFinishReason` **is** per-model (provider and model levels, `test/model-registry.test.ts:771-778`); what was not per-model was agentao's reason for inverting it (`INCOMPLETE_ANSWER_REASONS` values become CLI error envelopes, `pi-mono-pull-review-2026-08.md:58`). **Net: no precedent in this repo rules on the catalogue question**, and §9's "cannot maintain provider-neutrally" is withdrawn as unsupported — the entry is tabled on unmeasured demand, not on established impossibility; [P2] `enabled_tools` does **not** accept MCP names — the reserved-name guard rejects `mcp_` before the live-registry check (`agent.py:449-452`, pinned by `tests/test_host_tool_allowlist.py:138`); [P2] §9 still described the pi-mono/codex agreement as "about context cost" after §5 had downgraded that motive to inference — now shape-only; [P2] §4's axis is now **four** columns, not three: rev 5's version put registry *mutation* (`registerTool`/`unregisterTool`, `add_tool`) in the active-set column and then contradicted its own "no two harnesses vary on the same stage". The columns are *initial build* / *registry mutated after* / *active selection* / *schema projected*; **three of four mutate the registry post-build** (pi-mono rebuilds `_toolRegistry` on extension reload, `core/agent-session.ts:2664`), only pi-mono has an active-selection layer, only codex rebuilds per turn — and §10's "exactly one entry point" is narrowed to *initial* construction; [P3] the rev date is 2026-09-01. **rev 7's three:** [P1] **"the cost is a per-model catalogue" was itself an unproven constraint** on the top-priority finding — codex maintains one (mixed with `provider.capabilities()`, and `view_image` uses neither), but gemini-cli's gate is a **regex**, `/^gemini-2(\.|$)/.test(model)` (`config/models.ts:458`), its own comment calling it *"legacy behavior"*; Finding 1 and §9 now say the cost is *an owned, continuously maintained compatibility fact of some shape*, leaving a regex or per-provider flag available; [P2] the four-stage revision had not propagated — §0 still announced **three** stages "each on a different one", contradicting §4's own "the columns are not a partition", and §10's entry points were wrong in three places (pi-mono's `allToolNames` is a name set, not a build step; its real entry `_buildRuntime` is **re-run by `reload()`**, `core/agent-session.ts:2820`, so the table's "once" was wrong; agentao's full entry is `agent.py::_wire_tooling` at `:578`, not the `register_builtin_tools` it calls); [P2] "`max_tokens` is forwarded on every request" was too wide — `chat()`/`chat_stream()` default it to `None` (`llm/client.py:430,534`) and the kwarg is added only `if max_tokens` (`:419`), so the **main agent path** forwards it (`runtime/llm_call.py:138`) and the compaction summariser does not (`context_manager.py:1573`); the 65536 hazard is scoped to that path *and* to endpoints that clamp silently. **rev 8's one:** [P2] the four-stage table's **"schema projected" column answered *what* rather than *when*** for pi-mono and agentao — the timings are now in the cells, and they are the column's whole purpose, since this is what decides when a post-build registry mutation reaches the model. **rev 9's one, plus a peer defect it turned up:** [P2] rev 8 fixed two of those cells and **broke the third** — gemini-cli does *not* re-filter per request; `GeminiChat` caches its tool list, `registerTool` invalidates nothing (`core/src/tools/tool-registry.ts:271`), and the per-request `setTools(modelToUse)` returns early on an unchanged model (`core/src/core/client.ts:311-313`). Verifying that produced **Finding 6**: `reloadSkills()` re-registers `ActivateSkillTool` with a fresh skill enum and then calls only `updateSystemInstructionIfInitialized()` (`core/src/config/config.ts:3693-3699`), never `setTools()` — so after a runtime skill reload the model keeps being offered the **stale** enum until a model switch or explicit refresh. **agentao cannot reach that state**, because it re-projects from the live registry at the top of every `chat()`; the comparison's second peer defect, and the one that turns agentao's stricter snapshot into a safety property rather than just a stricter one. **rev 10's one:** [P3] this line said **180** citations for nine revisions; the real figure is **186**. The verifying script required a citation to be the **entire** backtick span, so it missed six: three written as `` `path:line :: symbol` `` and three unbackticked **tokens** (on two lines) in §10's fenced block. It reported low every round and the number was copied forward unexamined — the document's own "a self-referential statistic is not exempt from the failure mode it describes" rule biting the document. Two follow-on rounds were needed to settle it: **rev 11** because rev 10's explanation of the gap accounted for four of the six and had to be re-derived positionally, and **rev 12** because that correction was folded into rev 10's row instead of booked as its own round — the same accounting rule failing twice. Now stated with its counting rule so it is reproducible. en+zh twins, **196 identical `path:line` citations** — counted as every `path:line` token in the file, backticked or not, including §10's fenced block; both twins yield the same sequence. **192 of them machine-resolve to a unique non-blank line** across all four repos; the other four are deliberate — three are rev 1's *bad* citations quoted verbatim inside §10's basename method note, and one is the `<repo-root>/pyproject.toml` placeholder. **Every revision row that fixes a citation quotes the pre-fix spelling, and those spellings resolve** — which is each row's point, and why the count of "deliberately wrong" citations is stated as this rule rather than as a number that would drift with every such row. Across rev 3–9 the resolver caught six citation off-by-ones, five cross-repo basename collisions, one citation to a file that does not exist, and one paragraph silently lost to a batch script that raised before writing. **rev 13 is the first pass to read the cited lines rather than resolve them** — §3 and §4's 26 citations were checked against source at the anchors: every substantive claim substantiated, all four "quoted" strings verbatim, Finding 6 confirmed end to end (`setTools` is absent from the whole of `reloadSkills`, 3672–3709), and two precision defects fixed — a **live** citation violating §10's own bare-basename rule and crossing the package boundary §2.4 says must not be conflated, plus an off-by-two. Its method note is the transferable part: **the rev-10–12 resolver was looser than the claim it checked and so reported 184/186, *over* the asserted 182** — a verifier's number coming out better than the claim is a bug in the verifier. **rev 14 then applied §10's own bare-basename rule to the document for the first time** — 23 filenames were cited bare, five deliberately (§10's counter-examples plus rev 13's historical quotes) and **18 live**, all now qualified to be unique in their own repo (37 occurrences per twin; counts unchanged, since qualifying a path neither adds nor removes a citation). The transferable half is why rev 3–13's mechanical passes all reported clean: **a bare basename that happens to be unique in its repo resolves**, so no resolver could ever surface these — the rule needed a query of its own (list the distinct filenames cited; look at which carry no `/`), not a stricter resolver. **rev 15 content-checked §2 and §7 (58 citations, every substantive claim substantiated, four negative claims verified by repo-wide grep at the anchors) and found the rule itself too weak**: two range citations did not contain the sentence they quoted, and 13 citations were shorter than their repo's conventional root — including `handlers/view_image.rs` sitting beside `core/src/tools/handlers/apply_patch.rs`, two spellings of one directory, both resolving uniquely and therefore invisible to rev 14's sweep. §10's rule is now a **floor** (never shorter than the repo's root; over-qualifying allowed, and sometimes required). **rev 16 closed the document** — §0, §1, §6, §9 and §10 content-checked, §0 citing nothing and the rest reusing already-verified citations — and turned quote containment into a script: pull every `*"…"*` excerpt, find the nearest citation, join the cited lines with leading comment markers stripped, require containment. It caught three citations naming a line that did not hold the text quoted beside it, **one of which rev 13 had read by eye and passed** because the quotation does start on the cited line (`apply_patch.rs:73`, a doc comment running onto `:74`). Two limits are recorded with it: without the comment-marker strip a two-line `///` quotation reports as a miss, and the document's own italic phrasing is indistinguishable from a source excerpt, so one standing false positive is expected. **rev 17 checked the checks in** — all five are `scripts/check_citations.py`, which parses the anchors from the document's own `**Anchors:**` field so it cannot verify a tree the document does not name, exempts the revision-history table from the root floor (every such row quotes the pre-fix spelling as its evidence), and reports `SKIP` rather than `ok` when the peer worktrees are absent — it is a local tool, not a CI gate. Each check was falsified by injection before landing. Repairing §10 for it surfaced the round's one finding: rev 15's insertion had split rev 14's own explanation and stranded two sentences in the next bullet, **in the English twin only and invisible to every check**, since moving a sentence between paragraphs leaves the citation sequence unchanged. **Prose structure has no check** — the cell-count check covers tables, nothing covers paragraphs (2026-09-01).
- **otel-peer-survey** *(zh only)* — OpenTelemetry across 12 peer harnesses: **8 use OTel, 1 uses a non-OTel contract (pi-mono), 3 have none** — but the finding is the four *architectures* (in-core SDK / out-of-core extension / vendor-neutral contract / third-party wrapper), not the count. **Zero build items; does not change P1.2's demand gate** — the three conclusions are: don't start P1.2; when it starts, default-off + explicit endpoint + **all** OTel deps declared in an extra; keep the core event contract decoupled and defer JSONL-vs-live until a real enterprise topology exists. **§5 is a 13-row errata table from seven revisions — read it before re-raising anything**, and note the "none found" verdicts have a short shelf life (two upstream re-checks both flipped conclusions) (2026-08-15).
- **openworker-borrow-review** *(zh only)* — OpenWorker (andrewyng, aisuite-based desktop coworker) adoption assessment. **rev 3: zero borrow items approved.** The one action item is unrelated to OpenWorker — a **live permission fail-open found while cross-checking**: background sub-agents null out **every** legacy callback (`_wrapper.py:502`), so `agent.py:790` selects **`NullTransport`**, whose `confirm_tool` returns `True` (`null.py:28`) and an ASK-gated shell call runs silently (§1). **Corrected 2026-08-21** — the doc originally cited `sdk.py:101`, which is the *foreground* fail-open path; fix at the sub-agent, never by changing NullTransport/SdkTransport global semantics. The same review also un-specified §4's storage design (`sessions.py:142` writes a *new* timestamped file per save, not a stable atomic overwrite), gave §6 an explicit "do not introduce read containment" ruling scoped to the default `LocalFileSystem` (and added the two missed read sinks `search.py:173,357`), and dropped §10's §3/§4 reopen triggers. Two reverse passes falsified one finding (untrusted-content framing already exists in `prompts/sections.py`), declined the persistent shell **on process-management cost — not containment, since `path_policy.py:13` already excludes shell arguments**, downgraded the "capability catalog" to config hygiene, ruled RiskClass out, and caught **two items already settled** by `pi-mono-borrow-review` / `dynamic-workflows-review` — do not re-raise those without new evidence (2026-07-29).
- **dynamic-workflows-review** *(zh only)* — Claude Code Dynamic Workflows adoption assessment. **rev 5: don't adopt, don't design an alternative, change no code.** Records one verified fact (same tool instance serializes within a batch; whether that default may be bypassed is undefined) plus §3.5, a candidate implementation plan that activates only if a real use case demands it — explicitly not a commitment (2026-07-19).
- **code-mode-ptc-review** *(zh only)* — Code Mode / Programmatic Tool Calling decision record; 4 peer impls compared, current decision **not to start** pending demand (2026-07-09, 4th impl added 07-11).
- **codex-goal-mechanism-review** — two parts: Codex `/goal` mechanism teardown (§§1–9, descriptive) + agentao candidate design (§§10–11, **not approved**) (2026-06-23).
- **codex-subagent-v2-vs-agentao** *(zh only)* — **analysis only, not scheduled for implementation** (both P1s unauthorized as of 2026-08-21). codex Multi-Agent **V2** vs agentao sub-agents; **§1 is the whole conclusion**, the rest is its evidence. Core difference: codex keeps agents alive as actors that hold a residency slot until LRU eviction; agentao runs one-shot workers whose completion is injected as a `<system-reminder>` — **the same mechanism codex calls `inject_user_message_without_turn`**, minus a park primitive. If it is ever built, **two P1s** (a sub-agent's approval posture must never be lower than its parent's; an atomic background concurrency cap counting `_owned_ids` only), two observe (context truncation, polling cost), everything else do-not-build — park/mailbox and `fork_turns` included. Its §2 fail-open is **not a new finding** (`openworker-borrow-review` §1, 2026-07) and the measurement **narrows it**: the foreground path is not a second hole (production wiring always routes a sub-agent's ASK back to the parent transport) — only the background path downgrades, and `agentao run` flips its own fail-closed invariant one level down. Transport selection is **measured, with a runnable repro in §2**. §6 lists parity items — **do not re-flag those** (2026-08-21).
- **codex-compaction-vs-agentao** *(zh only)* — **the codex comparison is analysis only, not scheduled**; the one exception is its single P1, which is agentao-internal and **was implemented on 2026-08-23** (§3 「收口记录」 carries the measurements). **rev 8 after six maintainer review rounds — read §13, the 22-row errata table, before re-raising anything.** Of the draft's **two** P1s one was withdrawn and one survives; a third conclusion (filed as "open contract question", not a P1) was also withdrawn. **Scope is load-bearing**: codex's compact entry **dispatches four ways** (`tasks/compact.rs:36`) and `RemoteCompactionV2` is `default_enabled: true` (`features/src/lib.rs:1526`), so **for a provider that declares `V2`** the default path is remote v2, not the local `compact.rs` this doc compares against — a provider reporting `Unsupported` still goes straight to local, since the V2 arm is guarded by capability *and* feature (`tasks/compact.rs:41-43`). **That boundary is a provider *capability*, not a vendor** — Amazon Bedrock is not OpenAI yet reports `V2` (`amazon_bedrock/mod.rs:174-187`), and agentao users can point at the official OpenAI endpoint; local is the chosen comparison because it is the client-side-summarization mechanism, and because **agentao speaks Chat Completions, not Responses** (`llm/client.py:455,717`) — remote compaction is a different protocol surface, not a provider gap. The **one P1 was agentao-internal and stood even if nothing was borrowed** (now fixed — §3's 收口记录 records the data that decided the design): `_format_for_summary` truncates its input to 200 chars per tool result / 500 per message (`context_manager.py:529-530,649,651` — like every line number in this entry, anchored at `main@dc11298`; `context_manager.py` has moved since) while the prompt it feeds demands "Quote error messages **verbatim**" and "Be thorough" (`:549,551,563`) — and that is the *second* truncation, since full compression microcompacts the whole list first (`:368`). **Withdrawn**: `/model` vs ACP is **not** an asymmetry (a model-only ACP request calls the same `set_model()`), and PreCompact's non-blocking behavior is **not** deliberate Claude-Code parity — official docs confirm `PreCompact | Yes | Blocks compaction` (<https://code.claude.com/docs/en/hooks>, checked 2026-08-22), and `stop-precompact-hooks-plan.zh.md` already pinned agentao's gap as a **documented scope exclusion**. That plan is a frozen `docs/history/` archive and now carries a **2026-08-22 errata banner** (manual `/compact` shipped after it; `build_pre_compact` hardcodes `trigger:"auto"`, so manual compaction **mislabels itself** and no test covers that site). §10's token-budget entry has had its reason **wrong four times** (`WorldState` misattribution → "the whole window protocol is the prerequisite" over-claim → putting the model-owned reminders inside the default-off extension → calling those model defaults unconditional when they need no explicit host override *and* a model that ships them) and is the one cell to read in full before citing: it now separates **⓪ the whole mode being default-off** (`Feature::TokenBudget` is `UnderDevelopment, default_enabled: false`, `features/src/lib.rs:1413-1417`), **① the rollover core** (any path into `run_auto_compact` plus manual `/compact`, `turn.rs:1189` / `tasks/compact.rs:36`), **② model-owned reminder/guidance defaults** (`session/token_budget.rs:28` — needs the feature on, **no explicit host override** (`has_explicit_settings` returns early), **and** a model that actually ships `model_messages.token_budget`, which only 4 of 10 catalog entries do), and **③ the optional `history`/`notes` tools** (`use_history_notes_extension` defaults false *and* requires OpenAI provider + Codex backend auth). §11 is parity, §13 is the errata — **do not re-flag either** (2026-08-22).
- **pi-mono-compaction-vs-agentao** — **analysis only, nothing authorized for implementation**; §1 is a priority ordering of findings, not a schedule. **rev 6 after four maintainer reviews.** rev 3 landed 14 corrections (every one re-verified against source before acceptance); rev 4 **folded them into the body, tables and §13 and deleted the superseded verdicts**, because rev 3 had left several wrong statements standing beside their own errata — the doc asserted a claim and its refutation at once. §14 is now a **historical record, not an override**: the body is authoritative on its own. rev 5 tightened three surviving absolutes: a lumpy jump does **not** guarantee the cheap tier fires — `needs_microcompaction` is a **half-open** band `(55 %, 65 %]` (`est > 0.55×max` **and** `<= 0.65×max`, `context_manager.py:278-279`), so a jump straight past 130 K goes to planned full compaction instead; codex's silent-overflow blind spot is **threshold-dependent**, not total (a provider truncating and still reporting 99 % *does* trip it — only a reported value below the auto-compact threshold escapes), and pi-mono's `isRecoverableLength` needs `stopReason === "length"` so a silent truncation returning `stop` is outside it too (`ai/src/utils/overflow.ts:171`); and pi-mono's session persistence is the **default**, not unconditional — `--no-session` uses `SessionManager.inMemory` (`main.ts:358`, `session-manager.ts:1569`), where entries stay in the runtime tree and vanish with the process. rev 6 repaired two consistency slips that rev 5's own tightening introduced: the microcompaction band is **half-open**, `(55 %, 65 %]`, not "exclusive", and its citation pointed at `needs_compression` (`:256`) instead of `needs_microcompaction` (`:278-279`); and §13 still said a lumpy jump means "cheap tier still fires". Two-way pi-mono↔agentao comparison **except §3.3–3.4**, which bring in codex `2151d3a5b7` on the trigger axis alone. **rev 2's third P1 is demoted to P2 and re-scoped**: "the context window does not follow the model" was filed as an internal defect, but `max_context_tokens` is a **documented host-owned knob** on four surfaces (`agent.py:104`; `embedding/factory.py:132`; `cli-host-agent-factory.zh.md:104` names its owner "CLI 环境策略"; `acp-stdio-auth-fix-plan.md:99-110` defines model/contextLength/maxTokens as three knobs that **never overwrite each other**) — so the ACP behaviour rev 2 cited as *evidence of the defect* is in fact the written contract, and rev 2's route (c) "make it configurable" was **already implemented**. The framing sentence "both peers differing the same way is a gap" is **retracted**: peer convergence describes the option space, it does not convict a different point in it. What survives is real but narrower — the CLI applies one `200_000` default to every model (`cli/app.py:278`) and `/model` does **no validation, warning, or reconciliation** (`runtime/model.py:156`); with a configured window *larger* than the model's real one the two-tier design degrades into the emergency ladder, **conditionally** (not every turn — only when history re-crosses the real window; the cheap tier is skipped under *gradual* growth while a lumpy jump may land inside the band or overshoot it into full compaction; `messages[-2:]` can itself overflow so recovery is not guaranteed). A window *smaller* than the model's real one merely compacts early — rev 2's "any non-200 K model" was wrong in that direction. Standing P1s are **two**: `PreCompact` is notify-only (`_hook_dispatch.py:163`) against pi-mono's cancel-or-replace `session_before_compact` (`extensions/types.ts:592,1133`), and the circuit breaker has **no reset path** — `compress_messages` short-circuits at `context_manager.py:506` *above* the `= 0` reset at `:593`, and rev 3 corrected the rev-2 "parity" claim that manual `/compact` bypasses it: the check sits above any `is_auto` branch and the summary-failure increment (`:590`) is unconditional, so **the one path a user could use to recover is itself blocked**. rev 4 additionally corrected, in place: §13 still carried the retracted "unconditional internal P1" framing and the claim that the cheap tier is "never entered in practice"; the §3.3 three-way table still said 3-rung and claimed codex is "inherently covered" against silent overflow (it is not — a provider that truncates and reports the **post-truncation** usage is invisible to codex too; only pi-mono has a semantic check); §6.3 still said the carried summary is "n/a (separate input)"; §7's table still said the originals survive in replay+SQLite; §9.2 said "rest of the process" one line above its own instance-scoped correction; §9.3 still said `messages[-2:]` "guarantees forward progress" (it does not — the third call can return the error); and §11 still listed manual `/compact` as parity although §14 claimed it had been moved out. §3.2's "overflow recovery is a routine path" is now marked as what it is — a frequency inferred from a threshold, **unmeasured**, with `reserveTokens` being the mechanism that exists to prevent it. Replay is also not a byte-exact copy: `sanitize_event` redacts on the write path (`replay/recorder.py:135`). Other rev-3 corrections worth knowing before citing: agentao's overflow ladder is **2 rungs, not 3** (`_runner.py:1167,:1204`); codex's manual `/compact` is a **parallel dispatch**, not a sixth input to `run_auto_compact` (`handlers.rs:244` → `tasks/compact.rs:29`); pi-mono's previous summary is **one concatenated string**, not a separate structured input (`compaction.ts:670-680`); the reference extension **keeps the recent window** (`custom-compaction.ts:100-107`, whose own header comment contradicts its code); and compaction is irreversible **and unlogged on defaults** — `session_summaries` stores no originals (`memory/storage.py:44`), replay is off (`replay/config.py:36`), and a session save persists the compressed list (`embedding/sessions.py:145`). **Do not borrow** pi-mono's `chars/4` estimator, its unbounded summarization input, or its head-only tool-result truncation (§10); **do not borrow** codex's separate `ModelDownshift`/`CompHashChanged` sites, and **do not** move agentao's check to turn boundaries — on checking cadence agentao is level with codex and ahead of pi-mono (§12). The 65 % threshold is filed as **the user's call**, not a defect. §11 is parity, §14 is the errata — do not re-report either. Anchors pi-mono `a69bef789` / agentao `main@a996395` (2026-08-23); en+zh twins, 99 identical fully-qualified `path:lines` citations, checked mechanically (2026-08-23). **Acted on by** `docs/design/compaction-orchestration-plan.md` (`.zh.md` twin) — the six-PR plan that closes this doc's two P1s; reviewed twelve times, not authorized for implementation.
- **agent-definition-trust-line** *(zh only)* — open question, **neither side implemented**: project-level `.agentao/permissions.json` is refused on purpose, while project-level `.agentao/agents/*.md` loads unconditionally (`agents/manager.py:43`). **Not a privilege escalation** — definition frontmatter carries no policy fields and `tools` can only narrow — just the same trust line drawn two ways. (a) document why definitions are capability, not policy; (b) require explicit opt-in. Split out of codex-subagent-v2-vs-agentao on review so it stops widening that doc's scope (2026-08-21).
- **subagent-discovery-entrypoint-review** — sub-agent discovery asymmetry across the three entry points; corrects the skill-vs-plugin conflation the original report assumed (2026-06-23).
- **refactor-audit-2026-07** — full-tree refactor audit **plus an adversarial reverse review of its own findings**: 7 candidates → 3 shipped (#139/#140 + replay v1.2 render), 4 declined with evidence. Formally declines `optimization-opportunities-review`'s Tier 3 on churn data. Read the *declined* half first (2026-07-24/25).
- **optimization-opportunities-review** — multi-dimension audit (complexity / per-turn perf / duplication / packaging); Tier 1–3 findings, evidence-backed (2026-06-19). **Tier 1–2 shipped in v0.4.12; Tier 3 declined — see refactor-audit-2026-07 §5.**
- **vendor-sdk-convergence-review** — Claude/OpenAI Agent SDKs converged on Path A's pitch; §16.4 trigger determination (2026-06-18).
- **acp-server-conformance-review** — agentao ACP server vs official ACP v1; gaps G1–G6 + chat/automation target decision (2026-06-18).
- **core-boundary-review** — core vs host package-boundary audit (codex parallel, 2026-05).
- **codex-reverse-review** *(en only)* — reverse-review discipline record (2026-05-12).
- **pi-mono-borrow-review** — pi-mono v0.66→v0.73 borrow analysis; demand-gated precedent anchor.
- **pi-mono-pull-review-2026-08** — pi-mono v0.80.6→v0.83.0 (434 commits). 3 landed (#159 NFKC edit matching, #160 listener-error logging, #161 `finish_reason_missing` — the last **adopted inverted** from pi's error-by-default). 2 deferred as contract decisions (sub-agent boundary, ACP channel). `watch()` snapshot/subscribe recorded as the one real architectural gap, but its assumed trigger `agentao serve` is ✗-listed in the roadmap, so read §"Recorded, demand-gated" before assuming it is scheduled. 8 not-applicable, each with the query that settled it — **do not re-raise those without new evidence** (2026-08-02).
- **pi-mono-pull-review-2026-08-21** — pi-mono 2026-08-10→08-20 (176 commits). **rev 5, IMPLEMENTED 2026-08-21** (5 maintainer-review rounds; F2 then F1 — see the implementation note at the end of §4 for the four decisions the design left open and the three items still out of scope). Zero pi borrows; the value is two P1 defects in agentao's own config path. **D1:** `UnicodeDecodeError` subclasses `ValueError`, so none of the confirmed read sites catch it — `cli/app.py:387` runs before the factory, so a UTF-16LE `settings.json` (PowerShell 5.1's `>` default) kills CLI startup; missed sibling of a P0 `permission-hardening-plan.md` fixed one line lower. **D2:** rules are never validated — 7 type failures blow up **mid-turn** at `tool_planning.py:494`, and a `pattern`-for-`args` typo silently widens an allow-rule to the whole tool. §4 = the buildable route: **F1 and F2 are NOT independent** (layer table — `permissions.py::validate_permission_rules(rules)` is a pure **rule-list** validator with no notion of a JSON document; the loader owns document shape + path wrapping; shared by 3 callers); `permissions.json` **fails closed** at runtime but the **doctor path must catch and report** (`collectors.py:101` mirrors the loader — update it in the same change or it drifts); **schema validation is permission-rules-only** — the same uncaught-shape defect in `mcp.json` (top-level list → `AttributeError` out of `load_mcp_config`) and `skills_config.json` is recorded as a separate follow-up. Breaking: inverts `test_permissions_modes.py:280` and rewrites `configuration.md:130` **and both halves of** `:161` (case-insensitive kept; unknown `action` now **rejected**, not `ask`). **Three appendices are errata** — A: 5 first-pass errors (incl. `CONTEXT_COMPRESSED` **does** exist, on `transport/`); B: rev 1's rejected route (lesson: re-verify reachability for the *remedy*); C: rev 2's own 5 (lesson: before changing a behaviour, grep who documented and who tests it). Do not re-raise refuted claims from memory (2026-08-21).
- **pi-mono-tools-review** — pi-mono tools-level companion review.
- **pi-mono-openai-stream-fix** — pi-mono OpenAI-compat stream fix + agentao-side gap analysis.
- **system-prompt-profile** — host-injectable collaboration posture (review record; impl deferred).

---

### Where does a new doc go? · 新文档放哪一组？

- Analyzing another repo / auditing agentao / recording a decision → **Review & decision records**.
- Proposing or speccing work not yet shipped → **Active & proposed designs** (mark status in the `Status:` line).
- Documenting shipped behavior → **Landed contracts**.

When a doc graduates (proposal → shipped), move its line between groups here; don't move the file.
